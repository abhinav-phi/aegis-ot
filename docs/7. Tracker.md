# AEGIS-OT — Task Tracker

> **Last Updated:** 23 August 2026
> **Legend:** `[ ]` Todo · `[/]` In Progress · `[x]` Done · `[!]` Blocked
> Mirrors ImplementationPlan.md section IDs (1.1 ≈ `P1.1`, etc.). Requirement IDs refer to PRD.md.

---

## Phase 1 — Data + Baseline (Weeks 1–2)

### 🔧 Environment & Repo
- [ ] **P1.1** Environment + repo scaffolding (docker-compose, pyproject, Makefile, .env.example)
  - [ ] Setup monorepo w/ pinned deps (`pyproject.toml` + `uv.lock`)
  - [ ] docker-compose: postgres, minio, chroma, ollama, app
  - [ ] `make setup` works fresh-clone → all services healthy
  - [ ] CI lint + pytest skeleton green
- [ ] **P1.5x** Confirm SWaT dataset access + pin sha256
  - [ ] Download SWaT normal + attack runs (week 1, blocking risk)
  - [ ] Record sha256 in `datasets` registry

### 📦 Dataset (DATA-01,-02,-05,-06)
- [ ] **P1.2** Ingestion + registry
  - [ ] `pipeline/ingest/swat.py` → Parquet → MinIO
  - [ ] `pipeline/ingest/wustl.py` (secondary)
  - [ ] `pipeline/ingest/wadi.py` optional stub (DATA-03)
  - [ ] Registry rows w/ source, hash, sensor_columns
  - [ ] Verify row counts vs source

### 🧮 Data Pipeline (DATA-03,-04,-07,-08)
- [ ] **P1.3** Preprocessing + temporal splits
  - [ ] Cleaning (NaN/gap policy), z-score **train-fit only**
  - [ ] Windower W=60/S=1 → Parquet feature blocks
  - [ ] Train/validation/test split manifest (no shuffle)
  - [ ] `dataset_runs` rows + feature manifest + config_hash
  - [ ] Leakage test: train/test date ranges disjoint

### 🤖 Baseline (DET-02)
- [ ] **P1.4** Baseline detector + scoring
  - [ ] `iso_forest.py` window-level forest
  - [ ] `scoring.py`: score → τ via validation quantile
  - [ ] Latency capture (DET-05)
  - [ ] Baseline F1 table recorded (MLflow)
  - [ ] [STRETCH] `lstm_ae.py` ablation (DET-06)

**Phase 1 Verification:**
- [ ] `make pipeline-baseline` runs end-to-end on SWaT
- [ ] Sha256-pinned dataset registry populated
- [ ] Leakage test passes

---

## Phase 2 — Detection + Explainability (Weeks 3–5)

### 🚀 Detection Model (DET-03,-04,-06,-07)
- [ ] **P2.1** TCN-AE proposed detector
  - [ ] Dilated causal conv AE (train on normal windows)
  - [ ] Per-channel residual decomposition (attribution input)
  - [ ] `model_versions` + MLflow checkpoint per run
  - [ ] F1 vs baseline comparison table
- [ ] **P2.1x** [STRETCH] Transformer-AE ablation (DET-07)
  - [ ] `transformer_ae.py` curiosity run

### 📏 Invariants
- [ ] **P2.2** Invariant rules (DET-05)
  - [ ] 5 physics-bound rules implemented
  - [ ] Unit tests: crafted normal/anomalous windows

### 💡 Explainability (XAI-01,-02,-03)
- [ ] **P2.3** Attribution + explanation
  - [ ] Contribution bars (top-3 sensors)
  - [ ] Explanation object (hypothesis/evidence/invariants)
  - [ ] NL summary template + LLM polish (prompt pinned)
  - [ ] Consistency score (XAI-03)
  - [ ] Manual review of 20 sample anomalies

### 🧪 Evaluation 2
- [x] **P2.4** Stress protocol (EVAL-02) + fuzzy-rough channel-reduction arm (ROB-01/-02) *(executed on synthetic fixture 2026-08-23; licensed-data rerun pending)*
  - [x] `eval/stress.py`: noise / zeroing / drift augmentations (test-only, seeded)
  - [x] `eval/channel_reduction.py`: fuzzy-rough reduced-channel mask fit on TRAIN only
  - [x] Full vs reduced arms under the **identical** stress protocol (reduction % measured: persisted)
  - [x] Reduction mask + per-arm metrics persisted (`channel_reductions`, ROB-02)
  - [x] Robustness F1 curves vs baseline
  - [x] Report median across 3 seeds

**Phase 2 Verification:**
- [ ] Detector + attribution + explanation complete on SWaT
- [ ] Baseline vs proposed + stress table produced

---

## Phase 3 — Agentic RAG + Validator (Weeks 6–8)

### 📚 RAG + Threat Mapping (RAG-01..-05,-06, TINTEL-01)
- [x] **P3.1** KB + RAG + threat-mapping service *(corpus run executed 2026-08-23: hit-rate/MRR recorded; playbook authoring open below)*
  - [x] MITRE ATT&CK for ICS corpus ingest (trusted tier)
  - [x] Rule table + RAG match → `threat_mappings` rows w/ basis (TINTEL-01)
  - [x] SWaT docs corpus (trusted)
  - [x] Author 10+ team response playbooks (trusted tier, single named owner) → corpus *(11 playbooks authored 2026-08-28, SPD-017..SPD-027, covering pump/valve/flow/sensor/triage/recovery/escalation)*
  - [x] Chunker + embedder + Chroma collection (per run)
  - [x] Retriever w/ tier metadata + citations (RAG-03)
  - [x] `eval/kb_qa.py`: 20 canned queries → hit-rate/nDCG (RAG-04) — corpus run green
  - [x] Poison-harness for hostile chunking (RAG-06, used by attack suite)

### 🤖 Agent (AGENT-01..-06)
- [x] **P3.2** Agent loop *(owned deterministic ReAct runner; LangGraph deviation recorded per TechSpec "own the state")*
  - [x] `tools.py`: query_latest / query_history / search_kb / check_invariant / propose_action
  - [x] Runner loop w/ checkpointing + lease/interrupt-resume (DB lease model)
  - [x] Grounding system prompt (evidence-cited only; "insufficient data" fallback)
  - [x] Naive variant (AGENT-05)
  - [x] Seed scenarios traced + verified offline (scripted backend, 32-case suite)
- [ ] **P3.2x** [STRETCH] gpt-4o-mini cross-model (AGENT-06)

### 🛡️ Validator & Policy (SAFE-01..-04,-08)
- [x] **P3.3** Validator + policy engine
  - [x] Checks C1–C5 (provenance/allowlist/pattern/risk-class/consistency)
  - [x] `configs/policy/*.yaml` action grammar
  - [x] verdict → allow/require_approval/block/escalate
  - [x] Approval expiry scheduler (24h auto-escalate → `escalated`)
  - [x] Unit tests: action fixtures classified correctly (validator golden tests + 32-case suite)

### 🎯 Injection Suite (EVAL-04, THREAT-01..-04, SAFE-05)
- [x] **P3.4** Attack suite (families F1–F7)
  - [x] F1 poisoned history fixtures
  - [x] F2 document manipulation fixtures (hostile KB)
  - [x] F3 instruction leakage fixtures
  - [x] F4 narrative social engineering fixtures
  - [x] F5 tool-arg smuggling fixtures
  - [x] F6 hallucination probe fixtures
  - [x] F7 cyber-physical attribution-spoofing fixtures (numeric sensor zeroing/bias/scaling; no English text; simulation-only)
  - [x] ≥ 30 total cases; runner + per-case pass/fail output (32 cases)
  - [x] Naive vs hardened ASR + unsafe-action-rate tables

### 🧪 Sandbox (SAFE-04)
- [x] **P3.5** Sandbox simulator
  - [x] 6-stage SWaT-style surrogate plant model
  - [x] Action application + simulated telemetry delta
  - [x] Integration test: approve → simulate round-trip

**Phase 3 Verification:**
- [x] Hardened pipeline beats naive on injection suite *(offline scripted backend: ASR 0.1875 → 0.0)*
- [x] Approval gate blocks 100% of `control` actions without human

---

## Phase 4 — Integration + Evaluation + Demo (Weeks 9–10)

### 📊 Dashboard (UI-01..-06,-08)
- [x] **P4.1** Dashboard *(code-complete; interactive walkthrough pending live-stack review)*
  - [x] Telemetry + anomaly timeline (UI-01)
  - [x] Incident detail: attribution bars + explanation (UI-02)
  - [x] Agent reasoning/evidence panel (UI-03)
  - [x] Recommendation + validator panel (UI-04)
  - [x] Approval modal (UI-05)
  - [x] Attack-the-Agent demo stepper (UI-06)
  - [x] Audit viewer (UI-08)
  - [x] Design tokens per Design.md; responsive breakpoints

### 🧪 Full Eval (EVAL-01,-03,-06)
- [x] **P4.2** Experiment matrix EXP-01..EXP-09 *(all cells executable + executed on synthetic fixture via single CLI; licensed-data cells pending below)*
  - [x] EXP-01 baseline detector (synthetic fixture; SWaT cell license-gated)
  - [x] EXP-02 proposed detector (torch path verified end-to-end)
  - [x] EXP-03 ablation: no cyber-physical context
  - [x] EXP-04 ablation: no explanation
  - [x] EXP-05 naive agent
  - [x] EXP-06 grounded RAG agent
  - [x] EXP-07 RAG + validator agent
  - [x] EXP-08 adversarial/injection test suite (uses P3.4)
  - [x] EXP-09 human approval safety test (bypass battery)
  - [ ] WUSTL-IIoT-2021 secondary cells (key experiments) — license-gated
  - [x] Metrics → `evaluation_runs` + `metrics` + tables

### 🔬 Safety/RAG Metrics
- [x] **P4.3** Safety + grounding metrics (EVAL-05,-06)
  - [x] RAG citation correctness (re-run kb_qa in eval)
  - [x] Hallucination rate on unsupported questions — dedicated unsupported-question probe harness (`eval/hallucination_probe.py`, 7 questions; scripted offline: hallucination_rate=0.0 — the backend never fabricates; live-LLM refusal posture measurement pending)
  - [x] ASR / unsafe-action-rate / block rate / approval rate
  - [x] Latency numbers (DET inference ms measured in EXP-02; retrieval latency_ms recorded per event)

### 📝 Report & Demo
- [ ] **P4.4** Report + demo + defense
  - [ ] 8-page report (structure: background, methods, experiments, results, limitations)
  - [ ] Demo video: naive vs hardened walkthrough
  - [ ] Defense deck (12 slides)
  - [x] Progress record maintained in this Tracker across all phases

**Phase 4 Verification (PRD §9 Acceptance):**
- [x] Fresh-checkout `make pipeline-baseline` green *(verified on SQLite/local-store path; dockerized PG stack bring-up pending P1.1)*
- [x] Eval matrix regenerable from committed configs (`configs/experiments/*.yaml`, rerun-safe canonical runs)
- [x] Injection suite ≥ 30 cases pass/fail output (32 cases → `injection_cases`)
- [x] Demo stepper 7-step narrative functions (offline scripted backend; SIMULATED/FIXTURE labeled)
- [x] 100% of `control` actions require approval (validator verdicts + bypass battery)
- [/] Audit export contains full run trail — export endpoint + formula-injection escaping verified; a FULL demo trail (approval + sim rows) requires a backend that produces an approved plan (offline scripted arm refuses ⇒ no approval/sim rows). Re-verify on live stack.

---

## Cross-Cutting Concerns (Do Throughout)

- [x] `make test` green after every phase milestone (Step 8: 78 passed / 2 intentional skips / 0 failed / 0 errors)
- [/] Every commit builds (R37); meaningful messages (R21) — no VCS initialized in this workspace copy; applies to future commits
- [x] All metrics recorded in MLflow + `metrics` table (never only chat) — MLflow bridge wired in train path + EXP-02, fail-open offline
- [x] Raw telemetry never in PostgreSQL (R17) — object-store references only
- [x] No unsupported "SOTA" claims in any doc/slide (R41)
- [x] Update Tracker within 24h of any milestone (R38)
---

## Status Snapshot — Step 8 Hardening Complete (v1.2)

Legend per header. Updated against repository aegis-ot/ on 2026-08-23.

### Done [x] (implemented AND verified in repository)
- [x] **P3.0a** DB foundation: all hardened models (CHG-DB-01..20), Alembic 0001/0002/0003, plan-immutability listener + PG trigger, seed/bootstrap admin
- [x] **P3.0b** AuthN/Z: Argon2id, 15-min JWT, rotating hashed refresh w/ family revocation, login limiter, RBAC deps, audited set_role; structured error codes (AUTH-07 `invalid_credentials`)
- [x] **P3.0c** Core routers: auth/users/health/datasets/pipeline/telemetry/incidents(+close)/audit(+escaped CSV export)/eval
- [x] **P3.0d** Services: agent/approval/validator/sandbox with hash binding (INV-005), conditional transitions (session-consistent), distinct approver, deny frees run slot, amend from validated+approved revisions (DEC-002/R43)
- [x] **P3.0e** Workers: asyncio expiry scheduler + run reaper (no broker), heartbeat file for /health/worker *(loops exercised manually; heartbeat endpoint covered — see matrix row [/])*
- [x] **P3.1** RAG: chunker/embedder/vectorstore/retriever/trust-firewall/kb builder; TIER_DENIED recorded on every production denial path (INV-012); **RAG-04 corpus run executed** (`python -m eval.kb_qa`: hit-rate@5 + MRR recorded to metrics). Open sub-item: author 10+ playbooks (see plan §P3.1)
- [x] **P3.2** Agent: owned deterministic ReAct runner (scripted offline client + Ollama client); revision-safe draft materialization (UNIQUE(agent_run,revision) honored); NOTE recorded deviation — LangGraph replaced by owned loop per TechSpec rationale
- [x] **P3.3** Validator C1–C5 engine + strict policy grammar + fail-closed exceptions + persistent-C5 escalation; INV-010 naive lockout enforced in validator service too (no approvals ever created for naive plans)
- [x] **P3.5** Sandbox simulator (6-stage surrogate) with independent INV re-verification (authoritative row re-read defeats stale identity-map tamper), idempotent step resume
- [x] **P3.4** Attack suite: 32 fixtures across F1–F7 + runner writing injection_cases; EXP-08 green offline (naive ASR 0.1875 > hardened 0.0)
- [x] **P4.2** Experiment orchestration EXP-01..EXP-09 + STRESS-ROB via single CLI (`python -m eval.experiments --exp … [--dataset-run local]`); rerun-safe canonical run rows (EVAL-07); committed configs in `configs/experiments/*.yaml`
- [x] **P2.4** Stress sweep + ROB-01/02 EXECUTED on synthetic fixture: full vs reduced IF arms under identical seeded protocol; mask fit TRAIN-only; reduction % measured; persisted to `channel_reductions`
- [x] **P4.5** EVAL-08 pilot scaffolding (`eval/pilot.py`, ≤10 vignettes, exploratory-only, human-rating slots pending)
- [x] MLflow artifact wiring in train path (`app/core/mlflow_bridge.py`; fail-open offline)
- [x] `make pipeline-baseline` fresh-checkout path (fixture → preprocess → train → detect → metrics)
- [x] `make demo` (`eval/demo_runner.py`) — 7-step narrative offline, SIMULATED/FIXTURE labeled
- [x] Frontend foundation builds clean (tsc strict + vite): all pages per Design.md tokens
- [x] Lint debt cleared: ruff 256 → 15 findings, ALL remaining are deliberate fail-closed `except Exception` handlers (BLE001, R46 fail-closed semantics)
- [x] **P3.1** KB corpus complete: 11 team response playbooks authored (SPD-017..SPD-027, trusted tier, single named owner) — RAG-01 "≥10 playbooks" satisfied
- [x] **P4.3** Hallucination probe harness (`eval/hallucination_probe.py`, 7 unsupported questions; scripted offline: hallucination_rate=0.0 — no fabrication)
- [x] **EVAL-07** EXP-03 committed config added (`configs/experiments/EXP-03_ablation_no_cyber_physical.yaml`) — every experiment now has a committed YAML

### In progress [/]
- [/] **P1.x/P2.x pipeline modules**: end-to-end verified on the committed synthetic mini-fixture (EXP-01/02/STRESS-ROB measured); licensed-data runs blocked below

### Todo [ ] / Blocked [!]
- [!] P1.1/P1.5x docker bring-up verification + SWaT/WUSTL license-gated download & sha256 pinning — BLOCKED on external license acceptance + docker host; documented manual step (DEC-016)
- [!] WADI stress-only run (DATA-03 stretch) — license-gated
- [ ] Stretch ablations (DET-06/-07), gpt-4o-mini cross-model (AGENT-06), SHAP cross-check (XAI-05) — only after MUST HAVE green on licensed data
- [ ] Live Chroma adapter soak test against a running chroma service (LocalVectorStore path fully verified)

---

## Step 8 — Defect Resolution & Verification Record (2026-08-23)

Command: `python -m pytest -q` (SQLite in-memory; scripted LLM backend).
Result: **78 passed · 0 failed · 2 skipped(intentional) · 0 errors** — was 40 passed · 12 failed · 2 skipped · 26 errors at Step 7.

### Defects fixed (Step 7 list → resolution)

| # | Defect (Step 7) | Root cause | Fix | Verified by |
|---|---|---|---|---|
| 1 | scenario fixture omits config_hash ⇒ 26 errors | fixture predates NOT NULL config_hash (TEST-002) | conftest sets `config_hash="test-config-hash"`; teardown now rolls back residue from intentionally provoked IntegrityErrors instead of failing passing tests | full suite green |
| 2 | verdict ordering blocks citation-free-read allow | flags evaluated before whitelist input | verdict.py rewritten as pure lattice-max over unconditional rules; `citation_free_read` is an INPUT (VAL-001) | tests/validator 13/13 pass |
| 3 | kb.py missing RagDocument/RagChunk imports | stale module | imports present; NEW fix: chunks flushed before citation-id map (autoflush=False made by_id empty ⇒ KeyError) | trust-firewall + demo flow pass |
| 4 | retriever set−tuple TypeError | tier-diff arithmetic | list-intersection semantics; NEW fix: TIER_DENIED flag recorded on ALL denial paths incl. retrieval-unavailable (INV-012) | trust-firewall 5/5 pass |
| 5 | frontend 4 TS errors block build | 2 unused imports; incident type missing anomaly_id | removed imports; `anomaly_id: string` added to typed client | `npm run build` clean |
| 6 | Makefile app.db.migrate missing | module absent | `app/db/migrate.py` Alembic wrapper (+ `ensure_lite_schema()` SQLite bootstrap for offline CLIs) | migrate module imports; CLIs run |
| 7 | email fixtures single-label domain; metrics pop() side effect | test data + mutating comprehension | EmailStr-valid fixtures; pure metric test | rbac/metric tests pass |

### Additional defects found & fixed during Step 8 (deterministic checks)

| Area | Defect | Fix |
|---|---|---|
| State machine | conditional UPDATEs used `synchronize_session=False` → identity map stale; post-transition reads saw old statuses (broke approve/deny/retry chains) | `synchronize_session="evaluate"` — race-safe conditional update retained, session consistent |
| Approval service | `amend()` kwarg `actor` vs callers' `approver` (API router + tests) → TypeError at runtime | renamed to `approver`; amend now permitted from validated AND approved revisions; prior decision superseded same-tx (DEC-002/R43) |
| Deny flow | denied plan left originating agent run `running` forever → retry blocked (INV-015 slot leak) | deny() completes the run (rejected→analyzing retry per AppFlow §6) |
| Agent runner | `_materialize_draft` hardcoded revision_no=1 → UNIQUE(agent_run,revision) collision | next-revision computation per run (CHG-DB-07) |
| Agent runner | final transcript message written AFTER run completion transition → AGENT-001 stale-writer guard rejected it | record-then-transition ordering |
| INV-005 | raw-SQL row tamper invisible via stale ORM object → hash check passed on cached steps | authoritative `db.refresh(plan/approval)` before binding verification; test tamper made backend-portable |
| Sandbox | draft_only rejection message didn't name naive (INV-010 assertion) | split errors: `naive_plan_draft_only_not_executable` / `plan_status_superseded_not_executable` |
| Validator service | validation of naive plan created approval requests (INV-010/R44 violation) | naive plans: result recorded, status locked draft_only, zero approvals |
| Auth API | `AuthError("invalid_credentials")` rendered generic class code `unauthorized` | instance-level `code` override; AUTH-07 machine-readable codes |
| TCN-AE | `forward()` transposed an already-[B,S,T] input; residuals compared against untransposed windows; `load_bytes` referenced unimported `io` — model could NEVER run | shapes corrected; EXP-02 now trains/scores (F1 0.993 fixture-measured, 0.077 ms/window ≪ NFR-02) |
| Stress harness | `evaluate_robustness` raised unconditionally (`labels` never plumbed) and computed no metrics | full point-wise charter metrics per stressor × seed; τ from GT-normal clean scores only (EVAL-02/R23) |
| Channel reduction | `_triangular_membership` indexed 1-D centers with 3 indices → IndexError always | correct [N,C] membership math |
| Metric charter | `attribution_consistency` divided character distance by token count → unbounded negative scores | normalized by joined-string length, bounded [0,1] |
| Demo service | non-UUID created_by crashed UUID-typed FK columns; hard-created `Dataset(key='synthetic')` collided with registry | actor resolution helper + registry-row reuse (idempotent demo) |
| Missing imports (F821) | `Detection` in data.py telemetry endpoint, `MitigationPlan` in validator rerun endpoint, `io` in tcn_ae.load_bytes — runtime NameErrors | imported |
| Eval bookkeeping | rerunning identical (exp, config_hash) violated UNIQUE on completion | `_open_run` regenerates canonical row (purge old metrics/cases → failed → fresh) |

### Verification matrix (post-fix)

| Area | Implementation | Verification |
|---|---|---|
| DB models + migrations (CHG-DB-01..20) | [x] | [x] `alembic upgrade head` re-run green 2026-08-23 (0001→0002→0003, head) |
| AuthN/Z (JWT/refresh/RBAC/limiter/error codes) | [x] | [x] 7 pass · 1 intentional skip (inactive-login also blocked at login) |
| Plan hash binding / amendment | [x] | [x] 6/6 pass (incl. approved-revision amend, tamper HARD BLOCK) |
| Approval guards (expiry/replay/distinct) | [x] | [x] 3 pass · 1 intentional skip (no incident seeded) |
| Naive lockout (INV-010/R44) | [x] | [x] 3/3 pass (service + validator + sandbox layers) |
| Sandbox execution/idempotency | [x] | [x] pass (double-execute, closed-incident, step-failure escalation) |
| State machine edges (AppFlow §6) | [x] | [x] 7/7 pass |
| Validator C1–C5 | [x] | [x] 13/13 pass |
| RAG trust firewall (INV-012) | [x] | [x] 5/5 pass (TIER_DENIED on all paths) |
| Agent runner + scripted LLM | [x] | [x] 5/5 pass |
| Attack suite EXP-08 | [x] | [x] 32 cases; hardened ≥ naive holds |
| Bypass battery EXP-09 | [x] | [x] all attempts rejected |
| Metric charter | [x] | [x] bounded + deterministic |
| Pipeline service (preprocess/train/detect) | [x] | [x] exercised via EXP-01/02 + pipeline-baseline CLI |
| Workers (expiry/reaper) | [x] | [/] heartbeat endpoint covered; loops exercised manually only |
| Demo orchestration | [x] | [x] `make demo` offline round-trip green |
| Experiment matrix CLI | [x] | [x] EXP-01..09 + STRESS-ROB + RAG-04 + pilot executed on smoke DB |
| Frontend (tsc strict + vite) | [x] | [x] build clean |
| Lint (ruff) | [x] | [x] 15 remaining = deliberate fail-closed handlers (documented) |

### Honest scope notes (R25/R41/R42)
- Offline numbers are from the committed SYNTHETIC mini-fixture with the scripted LLM backend; they are reproducibility evidence, not licensed-SWaT results.
- Licensed SWaT/WUSTL/WADI cells remain manual, hash-pinned steps gated on dataset licenses.
- EVAL-08 pilot is scaffolding: human-rating slots are empty until study execution; reported exploratory only.

---

## Step 7 — Test Execution Record (2026-08-21)

Command: python -m pytest -q (SQLite in-memory; scripted LLM backend).
Result: **40 passed · 12 failed · 2 skipped · 26 error(tests)** — full report in audit log below.

### Verification verdicts (Implementation ≠ Verification)

| Area | Implementation | Integration | Verification |
|---|---|---|---|
| DB models + migrations (CHG-DB-01..20) | [x] | [x] | [x] alembic upgrade head green (0001→0002, 25 tables) |
| AuthN/Z (JWT/refresh/RBAC/limiter) | [x] | [x] | [ ] 5 endpoint tests fail on test-data domain (egis single-label rejected by EmailStr); logic unverified end-to-end |
| Plan hash binding / amendment | [x] | [x] | [!] all 5 binding tests ERROR via fixture bug (AgentRun.config_hash NOT NULL) |
| Approval guards (expiry/replay/distinct) | [x] | [x] | [!] 3 ERRORs same fixture cause; self-approval SKIPped (fixture) |
| Naive lockout | [x] | [x] | [!] 3 ERRORs same fixture cause |
| Sandbox execution/idempotency | [x] | [x] | [!] 4 ERRORs same fixture cause |
| State machine edges | [x] | [x] | [!] 7 ERRORs same fixture cause |
| Validator C1–C5 | [x] | [x] | [/] 10/12 pass; citation-free-read allow FAILS (verdict-order bug); C2/C4/C5 semantics pass |
| RAG trust firewall | [x] | [x] | [/] 1/4 pass; retriever set/tuple TypeError; kb.py missing model imports |
| Agent runner + scripted LLM | [x] | [x] | [/] draft materialization ERRORs (fixture); LLM decision tests pass |
| Attack suite EXP-08 | [x] | [x] | [x] runs green offline; hardened ≥ naive holds |
| Metric charter | [x] | n/a | [/] 8/9 pass; 1 test has self-inflicted pop() side effect |
| EXP-09 battery | [x] | [x] | [!] ERROR (fixture) |
| Demo orchestration | [x] | [ ] | [ ] NameError: kb.py imports |
| Pipeline service (preprocess/train/detect) | [x] | [ ] | [ ] not yet exercised by tests |
| Workers (expiry/reaper) | [x] | [ ] | [ ] loops not covered by tests |
| Frontend | [x] | [ ] | [ ] tsc: 4 errors (2 unused imports; incident type missing anomaly_id) → build blocked |
| make setup path (app.db.migrate CLI) | [ ] | — | [ ] module referenced by Makefile does not exist |

### Known defects for STEP 8 (do not fix in Step 7)
1. P1 tests/conftest.py — scenario fixture omits config_hash ⇒ 26 errors.
2. P1 pipeline/validator/verdict.py — flags checked before citation-free-read whitelist ⇒ read-only whitelisted actions cannot llow.
3. P1 pipeline/rag/kb.py — RagDocument/RagChunk not imported ⇒ NameError.
4. P2 pipeline/rag/retriever.py — set − tuple TypeError in tier-diff.
5. P2 frontend — 4 TS errors block 
pm run build.
6. P2 Makefile — python -m app.db.migrate target missing module.
7. P3 tests — email fixtures use single-label domain; metrics test uses mutating comprehension.

---

## Step 9 — Docs/Config Sync + RAG Completion Record (2026-08-28)

Scoped to the remaining code/config/QA items that are executable without licensed
SWaT/WUSTL data or live-infra soak tests (those remain manual, DEC-016).

### Work completed

| Item | What | Evidence |
|---|---|---|
| **P3.1 RAG-01** | Authored 11 team response playbooks (SPD-017..SPD-027, trusted tier) → KB corpus | `configs/kb/playbook_spd01[7-8].md`, `...spd01[9-27].md`; `tests/rag/test_kb_corpus.py` |
| **P4.3 EVAL-05** | Dedicated unsupported-question hallucination probe harness | `eval/hallucination_probe.py` (7 questions; scripted offline: hallucination_rate=0.0 — the deterministic backend never fabricates; refusal-posture measurement needs a live LLM); `tests/rag/test_hallucination_probe.py` |
| **EVAL-07** | EXP-03 committed config added — every experiment now has a committed YAML | `configs/experiments/EXP-03_ablation_no_cyber_physical.yaml`; `tests/unit/test_exp_configs.py` |
| **Docs sync** | Schema.md corruption fixed (NUL/control bytes + dropped chars in §8 — bad-save artifact); quick_summary.md refreshed to Step-8 state; TechSpec §2/§7/§8 + Design §10 record the frontend-stack/API/module reality as recorded deviations (project convention) | files above; diff vs Step 8 record |
| **RAG defect (found in this step)** | `LocalVectorStore.upsert` REPLACED the whole collection file per call, but `build_kb` upserts per document → production collection contained only the LAST document's chunks (offline/deterministic path; Chroma adapter merges, so the live path was unaffected). Fixed to merge (first-write-wins, dedupe by id) + `build_kb` re-indexes a wiped collection from DB rows (INV-016). RAG-04 corpus run now measures the full 14-doc corpus: hit-rate@5 0.5 / MRR 0.4625 (was effectively broken, single-doc collection) | `pipeline/rag/vectorstore.py`, `pipeline/rag/kb.py`; regression tests in `tests/rag/test_kb_corpus.py` |

### Verification

Command: `python -m pytest -q` — result after additions: all prior tests + 3 new test
files green (playbook corpus, hallucination probe, experiment-config presence).
`python -m eval.hallucination_probe` executes offline and writes metrics
(experiment_id `HALLUCINATION-PROBE`); `python -m eval.experiments --exp EXP-03
--dataset-run local` runs the no-cyber-physical-context ablation on the synthetic
fixture; frontend `tsc && vite build` unaffected (no frontend changes in this step).

### Honest scope notes (unchanged)

Licensed SWaT/WUSTL download + sha256 pinning, live Ollama/Chroma adapter soak,
EVAL-08 human ratings, and the report/video/deck deliverables remain pending —
none of this step fabricates real-data results (R25/R41).
